| Date | Change | Why | Approved by |
|------|--------|-----|-------------|
| 2026-09-17 | Repo skeleton created (include/, src/, Makefile) | Build baseline before implementation | M1, M2, M3 |
| 2026-09-19 | Exception header renamed SystemException.h -> Exceptions.h, source renamed to match | The include guard and every #include already said Exceptions.h; the file holds the whole hierarchy, not one class | M1 |
| 2026-09-19 | Enrolment.h and LectureCourse.h written (were empty placeholders) | Needed before M2 and M3 can use either class | M1 |
| 2026-09-19 | getCreditVal() body moved out of Course.h into Course.cpp | A function defined in a header is compiled into every .cpp that includes it, causing duplicate-symbol link errors | M1 |
| 2026-09-19 | Course keeps getTitle/getCapacity/getAssignedLectId/getPrereq/getEnrolledIds/getCreditVal/addPrereq, which the UML does not show | UML omits trivial accessors; keeping fields private with public getters is simpler than friend functions or protected fields, and Lecturer::viewEnrolmentList and Administrator::generateEnrolmentReport need the enrolment list | M1 |
| 2026-09-19 | Course::slots (vector<TimeSlot>) declared but commented out | TimeSlot is M2's class; the vector needs the complete type before Course can be constructed. Uncomment when M2 pushes TimeSlot.h | M1 |
| 2026-09-19 | Student's 4th constructor parameter accepted but unused | StudentCard is M2's class; the parameter stays in the signature so no call site changes when M2 lands | M1 |
| 2026-09-19 | Student now owns StudentCard and Timetable; added getCard, getTimetable, getCompletedCourses, addCompletedCourse | Required by UniversitySystem for UID lookup, clash checking and prerequisite checks | M1 |
| 2026-09-19 | Course now owns vector<TimeSlot> slots; added getSlots, addSlot; getPrereq renamed getPrerequisites | Matches the UML and the name UniversitySystem already calls | M1 |
| 2026-09-19 | All toLine() output changed from pipe to comma separators, and now includes every field | Pipe format could not be read back by the data files M3 wrote; save/load now round-trips byte for byte | M1 |
| 2026-09-19 | Person::fromLine, Course::fromLine and Enrolment::fromLine implemented | Repository<T>::load needs a T* factory per type; these decode the role/type tag and build the right subclass | M1 |
| 2026-09-19 | Person::getPassword() added (not on the UML) | toLine() must write the password or a save/load cycle would wipe every account's credentials | M1 |
| 2026-09-19 | Student card UID derived as "CARD_" + student ID | users.txt has no UID column, and replay_taps.txt uses CARD_S001 style tags | M1 |
| 2026-09-19 | ClashException now holds two TimeSlot objects instead of two strings | Matches the UML, and the message is built with TimeSlot's operator<< so clashes print as readable times | M1 |
| 2026-09-19 | Enrolment IDs generated as ENR<n>, counter advances past IDs already on disk | enrolments.txt already used ENR1/ENR2; without the bump a new enrolment would reuse an existing ID | M1 |
| 2026-09-19 | Timetable::addSlot(slot, courseId) and removeSlotsOf(courseId) added, with a parallel courseId vector | Both are on the UML and UniversitySystem calls them; dropping a course needs to know which slots came from it | M1 (M2 file - flagged to M2) |
| 2026-09-19 | src/AttendanceRecord.cpp written (constructor, getters, clone, toLine, fromLine, operator<<) | The header had no implementation file, so the project compiled but could not link | M1 |
| 2026-09-19 | AttendanceSession move constructor added | Declared in the header but never defined; vector<AttendanceSession> needs it when it reallocates | M1 |
| 2026-09-19 | Student/Lecturer/Administrator role methods now call UniversitySystem instead of printing 'not implemented' | The rules already existed in UniversitySystem; the role classes just had to delegate | M1 |
| 2026-09-19 | Menu extended with per-role submenus after login | Enrol, drop, timetable, attendance, sessions and reports were unreachable from main | M1 |
| 2026-09-19 | Menu input reads go through readChoice/readWord which stop on EOF | Closed or piped input made the old menu loop forever printing 'Invalid input' | M1 |
| 2026-09-19 | AttendanceRegister: shared recordTap() and countSessionsFor() helpers; AttendanceSession reformatted | Both processEvent overloads and three report functions repeated the same loops | M1 (M2 files) |
| 2026-09-20 | M2 files rewritten in house style: no std:: prefixes (using namespace std), no auto, no range-for loops, indexed loops throughout | Keeps every file readable in the same way, and uses only the standard C++ the module teaches | M1 (M2 files) |
| 2026-09-20 | AttendanceRegister::processEvent reduced to one version taking only a CaptureEvent; the std::function overload and <functional> removed | It now asks UniversitySystem for the UID lookup and enrolment check itself, which matches the single processEvent on the UML | M1 (M2 file) |
| 2026-09-20 | courseAttendanceReport merged into one function; the overload taking an enrolled-student list deleted | UML declares one report. It now fetches the roll from Course, so the caller no longer has to supply it | M1 (M2 file) |
| 2026-09-20 | FileReplayCapture::hasMoreEvents() removed | Not on the UML, not part of the AttendanceCapture interface, and never called - captureNext() already returns an EOF event | M1 (M3 file) |
| 2026-09-20 | Repository<T>::exists() removed | Never called anywhere; findById(id) != nullptr answers the same question | M1 (M3 file) |
| 2026-09-20 | UML corrections needed (diagram, not code): items is map<string, T*> not map<string, T>, all() returns vector<T*>, add takes T*, load takes the fromLineFactory function pointer, and remove() plus ~Repository() are missing | Storing objects by value would slice every subclass; the factory parameter is what lets one template build any type | M1 |
| 2026-09-20 | UniversitySystem::getEnrolments(), getDataDir(), findLecturer() and getEnrolmentsForCourse() removed | None had a single caller. Login uses getUsers().findById() plus dynamic_cast, and the lecturer's roll comes from Course::getEnrolledIds(), so the symmetric halves of both pairs were never needed | M1 (M3 file) |
| 2026-09-20 | docs/uml.puml added: the diagram now lives in the repo and includes UniversitySystem, which was missing entirely | It is the singleton that owns every repository and runs every enrolment rule, so a diagram without it hides the core of the design. Menu depends on UniversitySystem, not on Repository directly | M1 |
| 2026-09-20 | Card capture wired into the menu: opening a session now offers console / file / skip, and taps go through AttendanceCapture* to processEvent | processEvent had no caller anywhere, so no attendance could ever be recorded and every report read 0%. This is also the only place AttendanceCapture polymorphism appears at runtime | M1 |
| 2026-09-20 | A rejected tap is caught inside the capture loop | One duplicate or unenrolled tap must not end the whole session; the loop reports it and carries on | M1 |
| 2026-09-20 | Ten unused accessors removed: AttendanceRegister::getAllSessions (both), TimeSlot::getDay/getStartMin/getEndMin/getLocation, AttendanceSession::getSlot/getDurationMin, AttendanceRecord::getStatus/getTimestamp | None had a single call site. TimeSlot's own operator<< reads the fields directly because it is a friend | M1 (M2 files) |
| 2026-09-20 | docs/uml.puml updated to the final code | Diagram now matches what was built, including the missing UniversitySystem, the corrected Repository template, Menu's role submenus and the capture path | M1 |
| 2026-09-20 | Known gaps left in place: Administrator::createUser, updateUser, createCourse and editCourse still print 'not implemented', and 12 checks throw the base SystemException because no CourseNotFoundException type exists | Both sit in M1's own files and were left for the owner to decide; listed here so they are not mistaken for oversights | M1 |
